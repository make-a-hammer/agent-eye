---
tags:
  - 视觉搜索
  - AI眼睛
  - agent
  - 信息获取
  - 待实现
created: 2026-07-26
source: Hermes 对话 → 视觉后备通道设计
status: 设计完成 → 待开工
related: 
  - "[[AI-OS五层架构]]"
  - "[[AI信息获取三层架构]]"
---

# 视觉后备通道 · 设计文档

> 当 API 搜不到的时候，用眼睛看。
>
> 给 `search.py` 加一个视觉后备通道——API能搜的走API，搜不到的截图→多模态识别→返回。

---

## 一、定位

在 AI OS 五层架构中，这是**第一层（信息获取层）**的扩展：

```
现有：Tavily + arXiv API + GitHub API + 镜像站
新增：视觉后备通道（截图 → 多模态识别 → 结构化返回）
```

不是替代现有工具，而是**填上"没有API但有价值"的空白**。

---

## 二、核心逻辑

```
search.py 搜一个主题
   ↓
结果 ≥ 3 条 → 直接返回（现有逻辑不变）
   ↓
结果 < 3 条 → 触发 visual_fallback.py
   ↓
打开目标网页 → 截图 → 多模态识别 → 返回结构化结果
```

---

## 三、模块设计

### visual_fallback.py（~200行）

**输入：**
| 参数 | 类型 | 说明 |
|:---|:---|:---|
| `query` | str | 搜索关键词 |
| `urls` | list[str] | 要截图的网页列表（如Google Scholar搜索结果页） |
| `max_screenshots` | int | 最多截几张图（默认3，控制成本） |

**输出：**
与 `search.py` 完全相同的 `results` 列表：
```python
{
    "title": str,
    "url": str,
    "desc": str,        # 多模态AI提取的描述
    "info": str,        # 如 "📄 学术论文 | 123次引用"
    "source": "visual",  # 标记来源
    "type": "web",
    "download_url": str or None,
}
```

### 两个核心函数

```python
def screenshot(url: str) -> Image:
    """打开网页 → 截图 → 返回PIL Image"""
    # 用 Playwright 实现
    pass

def extract_from_screenshot(image: Image, query: str) -> list[dict]:
    """截图 → 多模态AI识别 → 返回结构化results"""
    # 调用豆包/Claude Vision API
    pass
```

---

## 四、技术选型

### 浏览器自动化：Playwright

```bash
pip install playwright
playwright install chromium
```

优势：API简洁、截图快、Python原生支持。

### 多模态识别：当前方案

| 方案 | 成本 | 状态 |
|:---|:---|:---|
| 豆包 API | 低 | ⚠️ 需确认是否有公开API |
| Claude Vision API | 中 | ✅ 你已有额度 |
| DeepSeek 多模态 | 低 | ⚠️ 取决于provider支持 |

**优先**：先查豆包API是否可用。
**兜底**：Claude Vision API。

### 成本预估

| 操作 | Token消耗 | 费用估算 |
|:---|:---|:---|
| 一次截图 → 识别 | ~5000 tokens | ~¥0.1-0.3 |
| 搜一个主题（3张截图） | ~15000 tokens | ~¥0.3-1 |
| 深度搜索（10个网页） | ~50000 tokens | ~¥1-3 |

> **原则**：不是每次搜索都动眼睛。API能搜到的，不触发视觉后备。

---

## 五、适用场景

| 场景 | 目标网页 | 提取什么 |
|:---|:---|:---|
| 学术文献 | Google Scholar 搜索结果 | 标题/作者/年份/引用量/PDF链接 |
| 技术文档 | 博客/教程页面 | 核心内容/代码块/版本号 |
| 软件下载 | 软件官网 | 版本号/下载链接/更新日志 |
| 论坛信息 | V2EX/Reddit/CSDN | 高赞回答/解决方案 |
| 闲鱼Track B | 豆瓣/出版社页面 | 书籍简介/目录/ISBN确认 |

---

## 六、实现路线

### 阶段一：最小可行（本次）

```
Playwright截图 → Claude Vision识别 → 返回结构化文本
```

1. `pip install playwright && playwright install chromium`
2. 写 `visual_fallback.py`
3. `search.py` 主函数加一行 fallback 触发逻辑

**不要做**：从头写浏览器驱动、自己训练模型、做万能识别。

### 阶段二：成本优化（后续）

- 豆包API接入（更便宜）
- 页面预处理（只截关键区域，减少token）
- 缓存（同一个URL不重复截图）

### 阶段三：自动化（远期）

- 集成到闲鱼Track B（视觉确认ISBN/目录）
- 与 agent-browser 或 browser-use 对接
- 做成 Hermes 可调用的 skill

---

## 七、与AI OS的关系

| AI OS层 | 视觉后备通道的位置 |
|:---|:---|
| ① 信息获取层 | **核心变化**——信息获取从"有API"扩展到"所有人眼可读" |
| ② 调度编排层 | search.py智能路由：API够用则不触发视觉 |
| ③ 记忆持久层 | 视觉搜到的结果也写入Vault |
| ⑤ 交付层 | Track B深度搜的能力得到实质性增强 |

---

## 八、关键约束

- **成本控制**：单次搜索最多3张截图，超出报"视觉搜索已达上限，请缩小范围"
- **速度预期**：一次视觉搜索比API搜索慢5-10秒，需在调用处有超时处理
- **隐私红线**：不截带有个人信息的页面（登录后页面、个人主页等）
- **范围限制**：只搜**公开可访问**的网页

---

## 相关笔记

- [[AI-OS五层架构]] — 本通道在信息获取层的定位
- [[AI信息获取三层架构]] — 信息获取层的完整架构
- [[软件+AI：快速下载与超级搜索]] — search.py 的设计思想
- [[思考者界面·打破线性输入边界]] — 多模态AI的哲学基础
